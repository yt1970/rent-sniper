# 🏠 rent-sniper 詳細設計

## 1. 🎯 目的

VillageHouseの東京都内物件を定期巡回し、新規空室・成約済み物件を検知してLINE通知する。
将来的にAIで「住みやすさコメント」を付与する。

---

## 2. 🧩 全体構成

```
flowchart TD
    A[main.py] --> B[list_page.py]
    B --> C[detail_page.py]
    C --> D[json_store.py / S3]
    D --> E[差分判定（新着・成約済み）]
    E --> F[ai/comment.py（実装中）]
    F --> G[line_notify.py]
```

---

## 3. 📦 モジュール設計

### 3.1 scraper/list_page.py

役割: 一覧ページから物件リンク取得

```python
def get_property_links() -> list[str]
```

入力: 一覧URL（`https://www.villagehouse.jp/chintai/kanto/tokyo/`）
出力: 物件URLのリスト

---

### 3.2 scraper/detail_page.py

役割: 各物件ページから詳細情報取得（Playwright使用）

```python
def get_rooms(property_url: str) -> list[dict]
```

出力フォーマット:
```json
{
  "id": "URL_物件名（一意キー）",
  "name": "ビレッジハウス〇〇",
  "url": "https://...",
  "address": "東京都〇〇区...",
  "line": "〇〇駅 - 徒歩X分",
  "status": "空室あり",
  "rent": "¥XX,XXX〜",
  "layout": "1K",
  "built": "YYYY/MM"
}
```

---

### 3.3 storage/json_store.py（S3対応版・本番用）

役割: S3からデータ読み書き・差分検知

```python
def load_previous() -> list[dict]   # S3から前回データ取得
def save_current(data: list[dict])  # S3に最新データ保存
def diff_rooms(old, new) -> list[dict]  # 新着物件を返す
```

> ローカル実行時は `json_store_local.py` を使うこと

---

### 3.4 ai/comment.py（実装中）

役割: 物件情報から評価コメント生成（OpenAI API使用予定）

```python
def generate_comment(room: dict) -> str
```

評価軸: 家賃・間取り・エリア・築年数

---

### 3.5 notifier/line_notify.py（本番用）

役割: LINE Messaging API で通知送信

```python
def send_line(message: str)
```

> ローカル実行時は `line_notify_local.py` を使うこと

---

## 4. 🔁 メイン処理フロー（main.py）

```
1. 一覧ページから物件URL取得
2. 各物件の詳細情報取得（Playwright）
3. S3から前回データ読み込み
4. 差分抽出
   ├── 新着物件（前回なし → 今回あり）
   └── 成約済み（前回あり → 今回なし）
5. LINE通知
   ├── 新着あり → 🏠 新着空室情報（最大5件）
   ├── 成約済みあり → 🔒 成約済みになりました（最大5件）
   └── 変化なし → 🏠 新着なし
6. S3に最新データ保存
```

---

## 5. ⏰ 実行方式

| トリガー | タイミング |
|---|---|
| cron | 毎月1日・15日 朝8時（JST） |
| push | mainブランチへのpush時 |
| 手動 | GitHub Actions の workflow_dispatch |

---

## 6. ☁️ AWS構成

| サービス | 用途 |
|---|---|
| S3 (`rent-sniper-data`) | 物件データ永続化（rooms.json） |
| IAM (`rent-sniper-github-actions`) | GitHub Actions用最小権限ユーザー |

IAM許可アクション: `s3:GetObject` / `s3:PutObject` / `s3:ListBucket`

---

## 7. ⚠️ エラーハンドリング

| ケース | 対応 |
|---|---|
| スクレイピング失敗 | 該当物件をスキップして継続 |
| LINE送信失敗 | ログ出力（ステータスコード・メッセージ） |
| S3接続失敗 | 例外をそのまま raise |
| AIコメント失敗 | コメントなしで送信（実装時に対応予定） |

---

## 8. 🔍 技術的リスク

| リスク | 内容 |
|---|---|
| HTML構造変更 | スクレイピング破綻 → セレクタ修正が必要 |
| JavaScript描画 | requests非対応 → Playwright で対応済み |
| アクセス制限 | IPブロック → User-Agent調整・間隔調整で対応 |

---

## 9. 🧪 デバッグ方針

```
① links → get_property_links() の出力確認
② room_data → get_rooms() の出力確認
③ new_rooms / sold_rooms → 差分確認
```

---

## 10. 📈 拡張案

- [ ] AIコメント生成の有効化（`ai/comment.py`）
- [ ] フィルタ機能（家賃上限・駅距離）
- [ ] スコアリング
- [ ] SQLite / RDS によるDB化
- [ ] MCPサーバー化
- [ ] Web UI
